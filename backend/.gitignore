# Dependencies
node_modules/

# Environment variables — NEVER push this
.env

# User uploaded files — ignore the FILES but keep the folders
uploads/songs/*
uploads/artists/*
uploads/directors/*
uploads/profiles/*
uploads/albums/*

# But always keep the .gitkeep placeholder files
!uploads/songs/.gitkeep
!uploads/artists/.gitkeep
!uploads/directors/.gitkeep
!uploads/profiles/.gitkeep
!uploads/albums/.gitkeep

# Logs
npm-debug.log*
*.log

# OS files
.DS_Store
Thumbs.db